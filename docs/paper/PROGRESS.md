# Paper Progress Tracker — Issue #16 (Springer IJIS submission, due 2026-09-08)

**Issue:** https://github.com/AI-Security-Internships-2026/02-soc-copilot-threat-analysis/issues/16
**PR:** https://github.com/AI-Security-Internships-2026/02-soc-copilot-threat-analysis/pull/18 (draft, opened 2026-08-04, based on `asma-week-09`; not ready for merge — Declarations content needs author sign-off, `docs/paper-draft.pdf` hasn't been copied to its final path yet, and the PR still needs retargeting to `dev`, see "Suggested next session's first move" below)
**Working draft:** `docs/paper/draft.md` (Markdown content draft) + `docs/paper/latex/ijis-draft.tex` (LaTeX port, started 2026-08-04, T1)
**Branch:** `worktree-paper-draft-issue-16`
**Last worked:** 2026-08-06

Read this file first when picking this up again. It tells you what's done, what's half-done, and exactly where to resume.

---

## How the sections map to issue #16's checklist

| Issue #16 item | Status | Where |
|---|---|---|
| Abstract (250 words) | Draft written (~230 words) | `draft.md` §Abstract |
| I. Introduction | Draft written | `draft.md` §I |
| II. Related Work | Draft written, all citations author-verified (R2 done) | `draft.md` §II |
| III. Proposed Method | Draft written, fully grounded in code + PRs, real pipeline figure | `draft.md` §III |
| IV. Evaluation | Draft written, **all items resolved** — E1 (2026-08-04), E2 (2026-08-06), E3 (2026-08-06) | `draft.md` §IV |
| V. Discussion | Draft written | `draft.md` §V |
| VI. Conclusion | Draft written (short — expand after IV/V settle) | `draft.md` §VI |
| References (≥20, majority 2023–2026) | **22, all verified real** (R1, R2 done 2026-08-06 — one fabricated entry from an earlier session was caught and replaced, see R2) | `draft.md` §References |
| Use journal's LaTeX/Word template | **Done (T1 resolved 2026-08-06)** — full section-by-section port done and compiling against the first-party-confirmed `svjour3` template | `docs/paper/latex/ijis-draft.tex` |
| All tables/figures referenced in text | **Done.** All 7 tables and all 3 figures (pipeline diagram, score distribution, throughput scaling) ported/generated and referenced (T2) | — |
| Results match committed files exactly | E1 and E2 both fully resolved | — |
| Commit draft to `docs/paper-draft.pdf` | **Started** — `docs/paper/latex/ijis-draft.pdf` compiles cleanly (verified with `tectonic`, 10 pages, no page limit found — T3 resolved) from the new LaTeX source; not yet copied/renamed to the final `docs/paper-draft.pdf` path since Declarations content still needs author sign-off (see "Still open" below) | `docs/paper/latex/ijis-draft.pdf` |
| Open PR against `dev` | Open against `asma-week-09` (retargeted 2026-08-06, see Branch note) — not yet against `dev` directly | — | 

---

## Open items to resolve before this is submission-ready

### Evaluation (E)
- **E1 — DONE (2026-08-04).** `experiments/schema_guardrail_eval.py` writes `experiments/results/schema_guardrail_eval.json`. Both halves are now committed: synthetic 20v20 (100% accuracy) and the real-data half (5,000 `AlertTitle` values from `datasets/GUIDE_train.csv`, 0 false positives) — the dataset was symlinked into this worktree from the main checkout to run it. `draft.md` §4.3 updated to cite the populated result instead of the PR #15 description, and the corresponding stale bullet removed from §V Limitations. **Caveat added to Limitations:** the script reads the first 5,000 rows in file order, not a random sample — flagged honestly rather than overclaiming "random sample."
- **E2 — DONE (2026-08-06), explained rather than rerun.** Traced `agent_metrics_real_v3.json` (and its siblings `real`, `real_check`, `real_v2`) via `git log --diff-filter=A` to Week 4–5 commits (`405c75c`, `d42e41a`/`0ef5d30`) — all predate the RF-fallback route (Week 6, `f145b8b`) and the schema guardrail / graph-wiring fix (Week 8–9, PRs #12/#14/#15/#17). Their baked-in "RF baseline: acc=0.374/f1=0.296" is not a bug: `git show d42e41a:experiments/results/baseline_metrics.json` confirms the baseline model genuinely scored that at `n_train=6,400`, before the Week 9 retrain (`ad02c85`) on the full split (`n_train=79,580`) produced the current 0.7718/0.7505 (matching `baseline_metrics.json` today). `agent_metrics_week6_fallback.json`/`_rerun.json` are also pre-schema-guardrail. Conclusion: `agent_metrics_post_graph_fix_week9.json` (n=30) is the *only* run against both the current pipeline and current baseline, so it's the number this paper uses; the older larger-n files are explained in `draft.md` §4.5 / `ijis-draft.tex` as non-comparable rather than cited as a bigger-sample alternative. No fresh eval was run — no `GROQ_API_KEY` or `baseline_model.joblib` available in this working environment; a larger post-fix run (e.g. n=150+) would still strengthen this section if credentials become available before submission.
- **E3 — DONE (2026-08-06).** `week7_scalability_benchmark.json` now has all 18 rows (LLM/RF/hybrid × 3 prompt-count tiers × 2 worker counts), sourced from PR #19 (`worktree-scalability-benchmark-rf-gap`, closes the RF and hybrid gaps flagged in the original Week 7 run — see `docs/weekly-progress.md`, "Scalability benchmark follow-up (2026-08-06)"). `draft.md` §4.6 and `ijis-draft.tex` §4.6 now transcribe the full per-mode tables. **Note:** this pulls in PR #19's data before #19 has merged to `dev` — same pattern as the existing #17 dependency below; re-verify the committed JSON matches `dev` once #19 lands. A throughput-vs-worker-count figure (T2c) is still not done.

### References (R)
- **R1 — DONE (2026-08-06).** Added 5 new references (18–22): MITRE ATT&CK text-tagging (`mitre_texttag2026`), adaptive incident prioritization (`incident_prioritization2026`), alert-fatigue survey (`alert_fatigue_survey2026`), scikit-learn (`pedregosa2011sklearn`), GUIDE Kaggle release (`guide_kaggle2024`) — all confirmed via direct fetch of the arXiv/publisher page before adding, none fabricated. Reference count is now 22. All 5 are cited inline (not just left in the bibliography) — see the Motivation paragraph (alert fatigue), the MITRE ATT&CK related-work paragraph (text-tagging, incident prioritization), §3.2 Data (Kaggle release), and §3.3 Step 1 (scikit-learn).
- **R2 — DONE (2026-08-06).** Verified every flagged reference (6, 9–16, plus `cortex2025`/`srinivas2025survey`'s abbreviated names) against arXiv/Crossref/publisher metadata via direct fetch. Two corrections surfaced along the way: `srinivas2025survey`'s journal was wrong (was "Informatics", is actually *Journal of Cybersecurity and Privacy*, DOI added) and `kgagent2025`'s year was wrong (was 2025, Crossref's published-online date is 2026). **One reference was found to be fabricated in an earlier session**: `ferrag2024` (arXiv:2407.08628, "Automated Cyber Threat Intelligence: LLMs for SIEM Alert Triage") — that arXiv ID resolves to an unrelated math.AP paper, and no real paper under that title exists anywhere. Replaced with the real, verified Ferrag et al. paper on arXiv (2405.12750, "Generative AI in Cybersecurity: A Comprehensive Review of LLM Applications and Vulnerabilities") — a broad review, not the specific SIEM/CICIDS2017 evaluation the fabricated entry claimed, so the two inline citations of this key (Introduction, Related Work) were rewritten to match what the real paper actually covers rather than keeping the false claim under a swapped citation key. This is the second fabrication caught in this paper's citation list this project cycle (see also the arXiv-ID sanity-checking this makes worth doing on *every* citation before submission, not just the ones already flagged) — a supervisor/co-author should do one more independent pass on all 22 references before submission regardless.

### Template and artifacts (T)
- **T1 — RESOLVED (2026-08-06), first-party confirmed.** The author fetched `link.springer.com/journal/10207/submission-guidelines` directly with an authenticated session and pasted the full page content in. It states plainly: *"Manuscripts should be submitted in LaTeX. Please use Springer's LaTeX macro package and choose the formatting option 'twocolumn'"*, with a specific **"LaTeX-Package (Download zip, 279 kB)"** download link. The author downloaded that zip (`1633537_svjour3-Latex-package.zip`) and shared it; it contains `svjour3.cls` + `svglov3.clo` (the generic "global" Springer journal class), `template.tex` (whose own `\documentclass[twocolumn]{svjour3}` line matches the guidelines' text instruction verbatim), and `svepjc3.clo` (a *different*, journal-specific class-option file for European Physical Journal C — **not used**, since IJIS isn't EPJC; this is what made an earlier attempt at this same zip look "not IJIS-specific" and get wrongly dismissed, see below).

  This **replaces** the earlier `sn-jnl.cls`-based port, which was a third-party-corroborated guess and turned out to be the wrong class entirely. The full re-port to `svjour3` is done: `docs/paper/latex/ijis-draft.tex` now uses `\documentclass[twocolumn]{svjour3}`, `svjour3.cls`/`svglov3.clo` are committed in `docs/paper/latex/`, the old `sn-jnl.cls`/`sn-*.bst`/`sn-article.tex` files are removed, and the bibliography is hand-written to match the guidelines' own numbered-bracket citation examples (confirmed against `svjour3.cls`'s default `\@lbibitem`/`\@citex` macros, which produce exactly that style with no `natbib` option needed). Compiles cleanly with `tectonic` — 10 pages, zero `undefined` warnings in the log after a full build. See `docs/paper/latex/README.md` for the full build/provenance note.

  **Retrospective note:** the 2026-08-06 follow-up attempt logged below (before the author got authenticated access) *did* find and download this exact `svjour3-Latex-package.zip` for journal 10207, but dismissed it as "generic, not IJIS-specific" because its readme mentions European Physical Journal C. That conclusion was wrong — the readme mentioning a different journal just reflects Springer sharing one generic template across a family of journals; it's still the file Springer's own current guidelines page serves for IJIS specifically. Lesson: a shared/generic-looking template can still be the officially linked one — the readme's journal name isn't evidence against that on its own.

- **T3 — RESOLVED (2026-08-06).** The full guidelines page (see T1) was read end-to-end: **there is no stated page or word limit anywhere** for a full research article — only Abstract (150–250 words) and Keywords (4–6) have explicit limits. The earlier "8–10 page" and "no IJIS-specific limit found" notes below were written without access to the real page; now that the real page has been read, the finding is that no limit exists to check the 10-page compiled draft against, not that one exists but couldn't be confirmed.

- **New from the T1 guidelines read — additional requirements not previously accounted for:**
  - Keywords: 4–6 required. Already satisfied (`ijis-draft.tex` has 6: SOC triage, LLM guardrails, prompt injection, negative results, LangGraph, MITRE ATT&CK).
  - ORCID ID: optional field on the title page, not yet added — needs the author's ORCID if they have one.
  - "Statements and Declarations" section (title-page instructions) requires, at minimum: Competing Interests (mandatory — submissions without it are returned as incomplete), plus Funding, Ethics/human-participants, Consent, and Data Availability as applicable. `ijis-draft.tex`'s Declarations section currently carries inherited "Not applicable" placeholders for Funding/Ethics/Consent and a generic no-competing-interests line — **none of these have been explicitly confirmed by the author**; flagged inline in the `.tex` file with a TODO comment. Data availability statement is now mandatory for all original research articles per Springer's Research Data Policy (also referenced on this page) — current wording (GUIDE dataset + repo link) should satisfy this but hasn't been reviewed against that policy specifically.
  - AI-assistance disclosure: the guidelines state LLM use beyond "AI assisted copy editing" must be documented in the Methods section. Added as a new `ijis-draft.tex` §3.5 ("AI-assistance disclosure") on 2026-08-06, per the author's explicit choice (full disclosure) — not previously present in any version of this draft.
  - Figures: guidelines prefer EPS (vector) or TIFF (halftone) for final artwork; the two generated figures (`score_distribution.png`, `throughput_scaling.png`) are PNG. Not yet addressed — likely fine as a draft but worth converting before final submission.

**Still open before this is submission-ready:**
- `docs/paper-draft.pdf` (the exact path the issue #16 checklist names) does not exist yet — only `docs/paper/latex/ijis-draft.pdf`. Needs copying/renaming once the Declarations content below is finalized (no point doing it before, since the PDF would need regenerating anyway).
- Declarations content needs explicit author sign-off, not inherited defaults: funding source (or confirmation there is none), competing interests, ethics/human-participants applicability, ORCID, and confirmation that "Asma Imran, TECIP, Scuola Superiore Sant'Anna" and the listed email are exactly correct.
- A final independent read-through of all 22 references before submission is still recommended (see R2 — one fabrication was already caught by doing exactly this).
- **T2 — DONE (2026-08-06), all three figures added.** (a) `ijis-draft.tex` §III.1 now has a TikZ figure (Figure~1) transcribed node-for-node/edge-for-edge from the compiled `StateGraph` in `src/agent/graph.py`, including the `rf_fallback`-skips-`parse_verdict` asymmetry; verified by rendering a standalone compile to PNG before merging into the main doc. `draft.md` keeps the ASCII version with a pointer to the LaTeX figure (Markdown can't render TikZ). (b) `docs/paper/figures/generate_figures.py` reads `soc_domain_eval_results.json`'s 40 per-row scores and plots a benign-vs-injection strip plot (`score_distribution.png`), embedded in both `draft.md` §4.2 and `ijis-draft.tex` §4.2 (Figure~2). (c) Same script reads the now-complete `week7_scalability_benchmark.json` (E3) and plots throughput vs. worker count faceted by mode with independent log-scale y-axes per panel (`throughput_scaling.png`), embedded in §4.6 (Figure~3) — one shared axis was rejected since the three modes differ by ~2 orders of magnitude (would violate the no-dual-axis rule from the dataviz method by proxy). Palette: first three slots of the validated categorical palette (blue/orange/aqua), chosen for all-pairs CVD safety. `matplotlib` added to `requirements.txt`. Regenerate with `python3 docs/paper/figures/generate_figures.py` from the repo root any time the source JSON changes.
- **T3 — DONE (2026-08-06), superseded by the T1 re-port below.** *(Stale as originally written — this entry predates the `svjour3` re-port and still described the old `sn-jnl.cls`/`sn-basic` port. Corrected 2026-08-08: see the T1 entry above for the current class, and the T3 entry above it for the current, accurate page-count finding.)* Compiled `ijis-draft.pdf` (with all figures/tables/22 references in place) is **10 pages** in the two-column `svjour3` layout (`tectonic ijis-draft.tex --print` reports "Output written ... (10 pages, ...)"). `draft.md` prose is ~4,200 words excluding tables, code fences, and the references list (~5,755 words total including everything). No IJIS-specific numeric page/word limit exists per the T1 guidelines read — see the T3 entry above for that finding. 10 pages in two-column `svjour3` is well within the normal range for a full research article in comparable Springer CS/security journals (typically 10–20 pages in that layout), so this is not a length concern.

---

## What's solid and doesn't need rework
- The core narrative (Sections III–V) is fully grounded in real, checked artifacts: `src/agent/schema_guardrail.py`, `src/agent/graph.py`, `src/agent/ml_guardrail.py`, PRs #12/#14/#15/#17, and `docs/weekly-progress.md` Weeks 8–9. No numbers in III/V were invented.
- Section IV's tables (4.1–4.6, all now complete, not partial) and figures (2, 3) are transcribed/generated directly from committed JSON files with the source file named next to each — safe to carry forward as-is.
- The Related Work framing (issue #16 asked specifically for LlamaFirewall, Prompt Guard, LLM Guard, and MITRE-ATT&CK-grounded LLM systems) is covered with real sources, not generic filler.

## Branch note
This work started on a worktree branch reset to `origin/asma-week-09` because that branch had the graph-wiring fix and regression tests this paper describes in §III.4 before `dev` did. **Update 2026-08-06:** PR #17 has since merged into `dev`, and `dev`/`asma-week-09` are now content-identical at their tips, so this PR's base was retargeted from `dev` to `asma-week-09` (metadata-only change, no rebase needed) to keep it grouped with Week 9's line of work. This PR still pulls in PR #19's (`worktree-scalability-benchmark-rf-gap`) scalability-benchmark JSON ahead of #19 merging — re-verify `week7_scalability_benchmark.json` matches once #19 lands on `asma-week-09`/`dev`.

## Suggested next session's first move
E1–E3, R1–R2, T1, T2, and T3 are all now done (2026-08-04 through 2026-08-06) — T1 and T3 resolved 2026-08-06 once the author got authenticated access to `link.springer.com/journal/10207/submission-guidelines` and the real `svjour3` template was found and ported (see T1 above). What's left, in order: (1) get the author's explicit answers on Declarations content (funding, competing interests, ethics/human-participants, ORCID, author-affiliation confirmation) rather than carrying inherited "Not applicable" placeholders; (2) copy the compiled `ijis-draft.pdf` to `docs/paper-draft.pdf` per the issue #16 checklist's exact path; (3) retarget PR #18 from `asma-week-09` to `dev` and take it out of draft, per the checklist's "Open PR against `dev`"; (4) once PR #19 merges, re-verify `week7_scalability_benchmark.json` still matches; (5) a final independent read-through of all 22 references before submission, since one (`ferrag2024`) already turned out to be fabricated in an earlier session and was only caught by deliberately re-verifying every flagged entry via direct fetch — see R2.
